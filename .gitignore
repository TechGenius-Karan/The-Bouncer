# Dependencies
node_modules/

# Build output
dist/
dist-ssr/

# Generated puzzle candidates (regenerable scratch output, not source)
content-engine/output/

# Local env
.env
.env.local
.env.*.local

# Logs
*.log
npm-debug.log*

# Browser-automation test session artifacts (snapshots/console logs)
.playwright-mcp/

# Editor
.vscode/*
!.vscode/extensions.json
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# OS
.DS_Store
Thumbs.db

# Local Netlify folder
.netlify
deno.lock

# Claude Code / agent tooling — local environment + installed skill caches,
# not project source (skills are reinstalled per-machine, worktrees and the
# scheduled-tasks lock are session-local state).
.claude/
.agents/
skills-lock.json
